{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="flash-{{ label }} alert alert-success">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}